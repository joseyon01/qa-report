import React from "react";
import Layout from "antd";
const { Header } = Layout;

export const HeaderLayout = () => {
  return (
    <Header>
      <span style={{ color: "#fff" }}>Componente Modificado</span>
    </Header>
  );
};
